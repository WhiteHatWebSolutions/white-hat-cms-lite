CMS_ENCRYPTION_KEY=replace-with-a-long-random-secret
# Comma-separated bootstrap owner addresses. Remove addresses after owners are
# represented in the CMS team table only if another owner remains available.
CMS_ADMIN_EMAILS=owner@example.com
# Canonical public URL used by metadata, feeds, robots, and exports.
PUBLIC_SITE_URL=https://cms.example.com
